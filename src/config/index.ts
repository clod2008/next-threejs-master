/**
 * Exportación de la configuración del proyecto
 */

import * as settings from './settings';
import * as types from './types';

export { settings, types };

// Exportación por defecto de la configuración
export default settings; 